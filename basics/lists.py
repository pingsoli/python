# List is ordered, so we can use index to access element in list.
# NOTE: the index of list is zero-based.

mylist = []
mylist.append(1)
mylist.append(2)
mylist.append(3)
#print(mylist[0])
#print(mylist[1])
#print(mylist[0])

# Traverse list 
for x in mylist:
    print(x)
